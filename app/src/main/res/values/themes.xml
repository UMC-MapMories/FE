<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MapMories" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.MapMories" parent="Base.Theme.MapMories" />
    <!-- Toolbar 스타일 -->
    <style name="Widget.MyTravelDiary.Toolbar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:background">@color/colorToolBar</item>
        <item name="android:titleTextColor">@color/black</item>
        <item name="title">@string/travel_diary</item>
        <item name="fontFamily">@font/rakkas</item>
        <item name="android:elevation">4dp</item>
    </style>

</resources>