<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- 둥근 모서리 적용 -->
    <corners android:radius="25dp"/>

    <!-- 배경 색상 설정 (하얀색) -->
    <solid android:color="@android:color/white"/>

    <!-- 테두리 설정 -->
    <stroke
        android:width="2dp"
        android:color="#96B3AF"/>

    <!-- 내부 패딩 설정 -->
    <padding
        android:left="16dp"
        android:right="16dp"/>
</shape>
